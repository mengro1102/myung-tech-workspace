# ── Python ───────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.Python
venv/
env/
*.egg-info/

# ── 환경변수 / 시크릿 ────────────────────────────
.env
.env.*
secrets.json

# ── 런타임 파일 ──────────────────────────────────
.pids
logs/
*.log

# ── 공유 메모리 이벤트 (런타임 생성) ──────────────
shared_memory/evt-*.json
shared_memory/tasks/

# ── 런타임 설정 (이 PC 의 선택) ────────────────────
# UI 에서 고른 공통 두뇌. 설치된 Ollama 모델에 따라 달라지므로 PC 마다 다르다.
runtime_config.json

# ── 에이전트 워크스페이스 (런타임 생성) ────────────
# 터미널 작업 디렉터리이자 SFT 데이터셋·Colab 노트북 출력 위치다.
workspace/

# ── Node.js ──────────────────────────────────────
node_modules/
npm-debug.log*
dist/
.vite/

# ── VS Code Extension 빌드 ────────────────────────
bridge/extension/out/
bridge/extension/node_modules/

# ── OS ───────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── IDE ──────────────────────────────────────────
.idea/
*.swp

# ── 모델 / 대용량 파일 ───────────────────────────
*.bin
*.safetensors
*.gguf
*.pt
*.pth

# ── 전처리 venv ───────────────────────────────────
preprocessing/venv/

# ── 브릿지 스냅샷 (런타임 생성) ──────────────────
bridge/bridge_state.json
# ── Claude 세션 파일 ──────────────────────────────
.claude/

# ── 빌드 산출물 ──────────────────────────────────
*.vsix
knowledge_base.py.bak.*

# 런처 교체 전 원본(WSL 기동판) — 참고용 로컬 보관
*.wsl-bak
*.bak
*.bak-*
